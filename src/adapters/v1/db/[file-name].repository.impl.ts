// contains the implementation of the repository for the [file-name] domain

import { FileNameRepository } from "../../domain/ports/[file-name].repository";

export class FileNameRepositoryImpl implements FileNameRepository {
  // add your repository methods here
  // for example:
  // findAll(): Promise<FileNameEntity[]> {
  //   return this.fileNameModel.find();  // a database query
  // }
  // findOne(id: string): Promise<FileNameEntity | null> {
  //   return this.fileNameModel.findById(id);  // a database query
  // }
  // create(fileNameEntity: FileNameEntity): Promise<FileNameEntity> {
  //   return this.fileNameModel.create(fileNameEntity);  // a database query
  // }
  // update(id: string, fileNameEntity: FileNameEntity): Promise<FileNameEntity> {
  //   return this.fileNameModel.findByIdAndUpdate(id, fileNameEntity, { new: true });  // a database query
  // }
  // delete(id: string): Promise<void> {
  //   return this.fileNameModel.findByIdAndDelete(id);  // a database query
  // }
}
